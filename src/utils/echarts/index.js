import getLineOption from './lines'

import {
  formatObjList,
  getCookiesData,
  setOptions,
} from './cookies'

export {
  // ? 折线图
  getLineOption,
  // ? 柱形图
  // ? 饼图
  formatObjList,
  getCookiesData,
  setOptions,
}