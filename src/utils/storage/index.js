// ? import functions
import {
  localSave,
  localSet,

  localRemove,
  localGet,
} from './localStorage'

// ? export functions
export {
  // ? local
  localSave,
  localSet,
  localRemove,
  localGet,
}