// ? import data
import {
  titleList,
  editContent,
} from './data'

// ? import functions
import {
  initDataList,
  formatContent,
  getMenuNameByKey,
  getDayKey
} from './functions'

export {
  // ? data
  titleList,
  editContent,

  // ? functions
  initDataList,
  formatContent,
  getMenuNameByKey,
  getDayKey
}