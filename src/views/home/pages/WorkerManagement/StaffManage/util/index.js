// ? data
import {
  titleList,
  itemOperations
} from './data';
// ? function
import {
  exportTableByComponent,
  handlePropItem,
} from './function';

export {
  // * data
  titleList,
  itemOperations,
  // * function
  exportTableByComponent,
  handlePropItem,
};